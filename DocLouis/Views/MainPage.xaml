<Page
    x:Class="DocLouis.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DocLouis"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
		<VisualStateManager.VisualStateGroups>
			<VisualStateGroup>
				<VisualState x:Name="wideState">
					<VisualState.StateTriggers>
						<AdaptiveTrigger MinWindowWidth="641" />
					</VisualState.StateTriggers>
				</VisualState>
				<VisualState x:Name="narrowState">
					<VisualState.StateTriggers>
						<AdaptiveTrigger MinWindowWidth="0" />
					</VisualState.StateTriggers>
					<VisualState.Setters>
						<Setter Target="inputButton.Margin" Value="0,4,0,0"/>
					</VisualState.Setters>
				</VisualState>
			</VisualStateGroup>
		</VisualStateManager.VisualStateGroups>
		
		<StackPanel x:Name="contentPanel" Margin="8,32,0,0">
			<TextBlock Text="Welcome to Doc Louis' Training Program" Margin="0,0,0,40"/>
			<TextBlock Text="What's the name of your training?"/>
			<TextBox x:Name="trainingNameInput" HorizontalAlignment="Stretch" Margin="8,20,8,20"/>
			<Button x:Name="inputButton" HorizontalAlignment="Stretch" Content="Configure Training" Click="Button_Click" Margin="8,0,8,0"/>
			<TextBlock x:Name="trainingNameOutput"/>
		</StackPanel>
	</Grid>
</Page>
